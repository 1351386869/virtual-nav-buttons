# Android 虚拟导航按键模拟器

这是一个简单的 Android 应用，在屏幕上显示三个虚拟导航按键（返回、主页、最近），点击后模拟系统导航操作。

## 功能
- 返回键（Back）
- 主页键（Home）
- 最近任务键（Recent Apps）

## 构建方式

需要 Android Studio 或命令行工具：
1. 用 Android Studio 打开项目
2. 或者使用 `gradlew assembleDebug` 编译

## 权限说明
- `SYSTEM_ALERT_WINDOW` - 悬浮窗权限（显示按键）
- 不需要 root 权限
